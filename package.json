{
  "name": "markdown-workspace-structure-generator",
  "displayName": "Markdown Workspace Structure Generator",
  "publisher": "Hedgehog420",
  "repository": "https://github.com/ToddHoward42069/markdown-workspace-structure-generator.git",
  "description": "This VSCode extension generates a Markdown Workspace structure from the current workspace.",
  "version": "0.0.6",
  "engines": {
    "vscode": "^1.88.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "markdown-workspace-structure-generator.generateMarkdown",
        "title": "Generate Markdown Workspace Structure"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.88.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "18.x",
    "eslint": "^8.57.0",
    "typescript": "^5.4.5",
    "@vscode/test-cli": "^0.0.9",
    "@vscode/test-electron": "^2.3.9"
  }
}
